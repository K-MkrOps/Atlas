apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atlas.builder.fullname" . }}-test-connection"
  labels:
    {{- include "atlas.builder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atlas.builder.fullname" . }}:{{ .Values.builder.service.port }}']
  restartPolicy: Never
