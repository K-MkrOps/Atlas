apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atlas.client.fullname" . }}-test-connection"
  labels:
    {{- include "atlas.client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atlas.client.fullname" . }}:{{ .Values.client.service.port }}']
  restartPolicy: Never
